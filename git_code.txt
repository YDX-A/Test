$ git config --global user.name "Your Name"
$ git config --global user.email "email@example.com"

$ mkdir 文件夹名
$ cd 文件夹名
$ pwd//显示目录：如果你使用Windows系统，为了避免遇到各种莫名其妙的问题，请确保目录名（包括父目录）不包含中文。


$ git init //通过git init命令把这个目录变成Git可以管理的仓库：

git diff //查看是否有更改，详细更改
git status//查看是否有 未添加，未提交。

git remote add origin git@github.com:YDX-A/learngit.git（创建连接）
git branch -M main
 git push -u origin main//上传所有
 git push origin main//上传仅更新的
git remote add origin git@github.com:YDX-A/Hello-Github